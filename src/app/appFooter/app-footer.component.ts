import { Component } from '@angular/core';

@Component({
  selector: 'app-footer',
  templateUrl: './app-footer.component.html',
  styles: [require('./app-footer.component.scss').toString()]
})
export class AppFooterComponent {
}